module example.com/user/hello


// Many hosting services already provide that metadata for repositories containing Go code,
//so the easiest way to make your module available for others to use is usually to make its module path match the URL for the repository.

go 1.16
